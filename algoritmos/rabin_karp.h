#ifndef RABIN_KARP_H
#define RABIN_KARP_H

#include <string>
#include <vector>

// Busca todas las ocurrencias de un patrón en el texto usando Rabin-Karp
std::vector<int> rabinKarpSearch(const std::string& text, const std::string& pattern);

#endif
